(deflisten music "scripts/music")

(defwidget music []
  (box :space-evenly false :class "music"
    (box
      :class "song-cover-art"
      :style "background-image: url('${music.cover}')")
    (label :text {music.title} :limit-width "24")
  )
)
